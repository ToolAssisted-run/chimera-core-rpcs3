diff --git a/rpcs3/Emu/Cell/PPUThread.cpp b/rpcs3/Emu/Cell/PPUThread.cpp
index 43a36adb4..4006a13bf 100644
--- a/rpcs3/Emu/Cell/PPUThread.cpp
+++ b/rpcs3/Emu/Cell/PPUThread.cpp
@@ -857,6 +857,35 @@ extern void ppu_register_function_at(u32 addr, u32 size, u64 ptr)
 	return ppu_register_function_at(addr, size, reinterpret_cast<ppu_intrp_func_t>(ptr));
 }
 
+#ifdef CHIMERA_CORE
+// Chimera: no page fault registers code on demand. Every table entry of a
+// freshly mapped page points here instead of at nothing; the first
+// execution decodes the page (the interpreter tail-chains through the table
+// without the loop's checks, so the entries must never be empty) and
+// returns to the dispatch loop, which re-dispatches at the same address.
+static void ppu_chimera_unregistered(ppu_thread& ppu, ppu_opcode_t, be_t<u32>* this_op, ppu_intrp_func*)
+{
+	const u32 addr = vm::get_addr(this_op);
+	if (getenv("CHIMERA_TRACE"))
+		fprintf(stderr, "[chimera] unregistered ppu code at 0x%x (this_op=%p base=%p)\n", addr, static_cast<const void*>(this_op), static_cast<const void*>(vm::g_base_addr));
+	ppu.cia = addr;
+	ppu_register_function_at(addr & -4096, 4096);
+}
+
+extern void ppu_fill_exec_table(u32 addr, u32 size)
+{
+	if (g_cfg.core.ppu_decoder == ppu_decoder_type::llvm)
+	{
+		return;
+	}
+
+	for (u32 a = addr & -4; a < addr + size; a += 4)
+	{
+		write_to_ptr_unsafe<ppu_intrp_func_t>(ppu_ptr(a), &ppu_chimera_unregistered);
+	}
+}
+#endif
+
 u32 ppu_get_exported_func_addr(u32 fnid, const std::string& module_name);
 
 void ppu_return_from_far_jump(ppu_thread& ppu, ppu_opcode_t, be_t<u32>*, ppu_intrp_func*)
diff --git a/rpcs3/Emu/Cell/SPUThread.cpp b/rpcs3/Emu/Cell/SPUThread.cpp
index d2b9e21cd..79a61fed2 100644
--- a/rpcs3/Emu/Cell/SPUThread.cpp
+++ b/rpcs3/Emu/Cell/SPUThread.cpp
@@ -1740,6 +1740,7 @@ enum : s64
 
 spu_thread::~spu_thread()
 {
+#ifndef CHIMERA_CORE
 	// Unmap LS and its mirrors
 	for (s64 ls_offs = 0 - SIGNED_LS_SIZE * 2; ls_offs <= SIGNED_LS_SIZE * 2; ls_offs += SIGNED_LS_SIZE)
 	{
@@ -1747,6 +1748,7 @@ spu_thread::~spu_thread()
 	}
 
 	utils::memory_release(ls - SIGNED_LS_SIZE * 3, SIGNED_LS_SIZE * 7);
+#endif
 }
 
 u8* spu_thread::map_ls(utils::shm& shm, void* ptr)
@@ -1881,7 +1883,12 @@ spu_thread::spu_thread(utils::serial& ar, lv2_spu_group* group)
 	, index(ar)
 	, thread_type(group ? spu_type::threaded : ar.pop<u8>() ? spu_type::isolated : spu_type::raw)
 	, shm(ensure(vm::get(vm::spu)->peek(vm_offset()).second))
+#ifdef CHIMERA_CORE
+	// Chimera: local storage is the guest view itself (no mirror ring)
+	, ls(vm::_ptr<u8>(vm_offset()))
+#else
 	, ls(map_ls(*this->shm))
+#endif
 	, option(ar)
 	, lv2_id(ar)
 	, spu_tname(make_single<std::string>(ar.operator std::string()))
diff --git a/rpcs3/Emu/Memory/vm.cpp b/rpcs3/Emu/Memory/vm.cpp
index aeed9d051..a36c0c2c8 100644
--- a/rpcs3/Emu/Memory/vm.cpp
+++ b/rpcs3/Emu/Memory/vm.cpp
@@ -24,6 +24,10 @@ LOG_CHANNEL(vm_log, "VM");
 void ppu_remove_hle_instructions(u32 addr, u32 size);
 extern bool is_memory_compatible_for_copy_from_executable_optimization(u32 addr, u32 size);
 
+#ifdef CHIMERA_CORE
+extern void ppu_fill_exec_table(u32 addr, u32 size);
+#endif
+
 namespace vm
 {
 	static u8* memory_reserve_4GiB(void* _addr, u64 size = 0x100000000, bool is_memory_mapping = false)
@@ -39,6 +43,36 @@ namespace vm
 		fmt::throw_exception("Failed to reserve vm memory");
 	}
 
+#ifdef CHIMERA_CORE
+	// Chimera: no shared memory, so no mirror - the "sudo" view IS the guest
+	// view and every mapped page is readable and writable. The flat 4 GiB
+	// guest view and the 8 GiB (+2 GiB segment map) execution table stay
+	// flat; the hook and stat areas are dead (hooks) or debug-only (stats)
+	// and shrink to a page so the sandbox's address space is not spent on them.
+	// vm::get_addr recovers a guest address from a host pointer by truncation,
+	// so the guest view must sit on a 4 GiB boundary: reserve twice the size
+	// and keep the aligned half.
+	static u8* reserve_aligned_4GiB()
+	{
+		u8* const raw = static_cast<u8*>(ensure(utils::memory_reserve(0x2'0000'0000, nullptr, true, false)));
+		u8* const aligned = reinterpret_cast<u8*>((reinterpret_cast<uptr>(raw) + 0xffff'ffff) & ~uptr{0xffff'ffff});
+		if (aligned > raw)
+		{
+			utils::memory_release(raw, aligned - raw);
+		}
+		if (aligned + 0x1'0000'0000 < raw + 0x2'0000'0000)
+		{
+			utils::memory_release(aligned + 0x1'0000'0000, (raw + 0x2'0000'0000) - (aligned + 0x1'0000'0000));
+		}
+		return aligned;
+	}
+
+	u8* const g_base_addr = reserve_aligned_4GiB();
+	u8* const g_sudo_addr = g_base_addr;
+	u8* const g_exec_addr = static_cast<u8*>(ensure(utils::memory_reserve(0x2'8000'0000, nullptr, false, false)));
+	u8* const g_hook_addr = static_cast<u8*>(ensure(utils::memory_reserve(0x10000, nullptr, false, false)));
+	u8* const g_stat_addr = static_cast<u8*>(ensure(utils::memory_reserve(0x10000, nullptr, false, false)));
+#else
 	// Emulated virtual memory
 	u8* const g_base_addr = memory_reserve_4GiB(reinterpret_cast<void*>(0x2'0000'0000), 0x2'0000'0000, true);
 
@@ -53,6 +87,7 @@ namespace vm
 
 	// Stats for debugging
 	u8* const g_stat_addr = memory_reserve_4GiB(g_hook_addr);
+#endif
 
 	// For SPU
 	u8* const g_free_addr = g_stat_addr + 0x1'0000'0000;
@@ -809,10 +844,14 @@ namespace vm
 		}
 
 		auto prot = utils::protection::rw;
+#ifndef CHIMERA_CORE
+		// Chimera: pages are uniformly RW (no mirror to reach "hidden" pages
+		// through); g_pages still records the logical permissions
 		if (~flags & page_writable)
 			prot = utils::protection::ro;
 		if (~flags & page_readable)
 			prot = utils::protection::no;
+#endif
 
 		std::string map_error;
 
@@ -845,10 +884,20 @@ namespace vm
 			fmt::throw_exception("Memory mapping failed (addr=0x%x, size=0x%x, flags=0x%x): %s", addr, size, flags, map_error);
 		}
 
+#ifdef CHIMERA_CORE
+		// Chimera: no page faults commit the execution table on demand, so
+		// every mapped page gets its table pages now, filled with the stub
+		// that decodes a page on first execution
+		if (!is_noop)
+#else
 		if (flags & page_executable && !is_noop)
+#endif
 		{
 			// TODO (dead code)
-			utils::memory_commit(g_exec_addr + addr * 2, size * 2);
+			utils::memory_commit(g_exec_addr + u64{addr} * 2, u64{size} * 2);
+#ifdef CHIMERA_CORE
+			ppu_fill_exec_table(addr, size);
+#endif
 
 			if (g_cfg.core.ppu_debug)
 			{
@@ -929,11 +978,13 @@ namespace vm
 						g_pages[j].release(start_value);
 					}
 
+#ifndef CHIMERA_CORE
 					if ((old_val ^ start_value) & (page_readable | page_writable))
 					{
 						const auto protection = start_value & page_writable ? utils::protection::rw : (start_value & page_readable ? utils::protection::ro : utils::protection::no);
 						utils::memory_protect(g_base_addr + start * 4096, page_size, protection);
 					}
+#endif
 
 					range_lock->release(0);
 				}
@@ -1038,9 +1089,13 @@ namespace vm
 #endif
 		}
 
+#ifdef CHIMERA_CORE
+		if (!is_noop)
+#else
 		if (is_exec && !is_noop)
+#endif
 		{
-			utils::memory_decommit(g_exec_addr + addr * 2, size * 2);
+			utils::memory_decommit(g_exec_addr + u64{addr} * 2, u64{size} * 2);
 
 			if (g_cfg.core.ppu_debug)
 			{
@@ -2246,7 +2301,10 @@ namespace vm
 #ifdef _WIN32
 			utils::memory_release(g_hook_addr, 0x800000000);
 #endif
+#ifndef CHIMERA_CORE
+			// Chimera: the hook area is dead code and shrunk to a page
 			ensure(s_hook.map(g_hook_addr, utils::protection::rw, true));
+#endif
 		}
 	}
 
@@ -2268,10 +2326,16 @@ namespace vm
 		}
 
 		utils::memory_decommit(g_exec_addr, 0x200000000);
+#ifdef CHIMERA_CORE
+		utils::memory_decommit(g_stat_addr, 0x10000);
+#else
 		utils::memory_decommit(g_stat_addr, 0x100000000);
+#endif
 
 #ifdef _WIN32
+#ifndef CHIMERA_CORE
 		s_hook.unmap(g_hook_addr);
+#endif
 		ensure(utils::memory_reserve(0x800000000, g_hook_addr));
 #else
 		utils::memory_decommit(g_hook_addr, 0x800000000);
diff --git a/rpcs3/util/vm_native.cpp b/rpcs3/util/vm_native.cpp
index 5b5c7746f..9073f7105 100644
--- a/rpcs3/util/vm_native.cpp
+++ b/rpcs3/util/vm_native.cpp
@@ -224,6 +224,10 @@ namespace utils
 
 	void* memory_reserve(usz size, void* use_addr, [[maybe_unused]] bool is_memory_mapping, [[maybe_unused]] bool can_be_jit)
 	{
+#ifdef CHIMERA_CORE
+		// the sandbox places reservations; hints mean nothing there
+		use_addr = nullptr;
+#endif
 #ifdef _WIN32
 		if (is_memory_mapping && has_win10_memory_mapping_api())
 		{
@@ -461,6 +465,11 @@ namespace utils
 		{
 			return true;
 		}
+#ifdef CHIMERA_CORE
+		// nothing to pin: the sandbox's pages are never swapped out of it
+		static_cast<void>(pointer);
+		return true;
+#endif
 
 #ifdef _WIN32
 		return ::VirtualLock(pointer, size);
@@ -490,6 +499,14 @@ namespace utils
 		: m_flags(flags)
 		, m_size(utils::align(size, 0x10000))
 	{
+#ifdef CHIMERA_CORE
+		// Chimera: a "shared memory" object is just a size; each map commits
+		// its own pages (two mappings of one object do not alias - the base
+		// and "sudo" views are the same view, and true guest aliasing waits
+		// for a title that needs it)
+		m_file = -1;
+		return;
+#endif
 #ifdef _WIN32
 		const ULARGE_INTEGER max_size{ .QuadPart = m_size };
 		m_handle = ensure(::CreateFileMappingW(INVALID_HANDLE_VALUE, nullptr, PAGE_EXECUTE_READWRITE, max_size.HighPart, max_size.LowPart, nullptr));
@@ -533,6 +550,11 @@ namespace utils
 	shm::shm(u64 size, const std::string& storage)
 		: m_size(utils::align(size, 0x10000))
 	{
+#ifdef CHIMERA_CORE
+		static_cast<void>(storage);
+		m_file = -1;
+		return;
+#endif
 #ifdef _WIN32
 		fs::file f;
 
@@ -803,6 +825,9 @@ namespace utils
 
 	shm::~shm()
 	{
+#ifdef CHIMERA_CORE
+		return;
+#endif
 		this->unmap_self();
 
 #ifdef _WIN32
@@ -817,6 +842,20 @@ namespace utils
 
 	u8* shm::map(void* ptr, protection prot, bool cow) const
 	{
+#ifdef CHIMERA_CORE
+		static_cast<void>(cow);
+		u8* target = static_cast<u8*>(ptr);
+		if (!target)
+		{
+			target = static_cast<u8*>(memory_reserve(m_size, nullptr, false, false));
+			if (!target)
+			{
+				return nullptr;
+			}
+		}
+		memory_commit(target, m_size, prot);
+		return target;
+#endif
 #ifdef _WIN32
 		DWORD access = FILE_MAP_WRITE;
 		switch (prot)
@@ -886,6 +925,9 @@ namespace utils
 
 	u8* shm::try_map(void* ptr, protection prot, bool cow) const
 	{
+#ifdef CHIMERA_CORE
+		return this->map(ptr, prot, cow);
+#endif
 		// Non-null pointer shall be specified
 		const auto target = ensure(reinterpret_cast<u8*>(reinterpret_cast<u64>(ptr) & -0x10000));
 
@@ -905,6 +947,10 @@ namespace utils
 
 	std::pair<u8*, std::string> shm::map_critical(void* ptr, protection prot, bool cow)
 	{
+#ifdef CHIMERA_CORE
+		u8* res = this->map(ptr, prot, cow);
+		return {res, res ? std::string{} : std::string("commit failed")};
+#endif
 		const auto target = reinterpret_cast<u8*>(reinterpret_cast<u64>(ptr) & -0x10000);
 
 #ifdef _WIN32
@@ -994,6 +1040,13 @@ namespace utils
 
 	u8* shm::map_self(protection prot)
 	{
+#ifdef CHIMERA_CORE
+		if (!m_ptr)
+		{
+			m_ptr = this->map(nullptr, prot, false);
+		}
+		return static_cast<u8*>(+m_ptr);
+#endif
 		void* ptr = m_ptr;
 
 		for (void* mapped = nullptr; !ptr;)
@@ -1021,6 +1074,10 @@ namespace utils
 
 	void shm::unmap(void* ptr) const
 	{
+#ifdef CHIMERA_CORE
+		memory_decommit(ptr, m_size, false);
+		return;
+#endif
 #ifdef _WIN32
 		::UnmapViewOfFile(ptr);
 #else
@@ -1031,6 +1088,10 @@ namespace utils
 	void shm::unmap_critical(void* ptr)
 	{
 		const auto target = reinterpret_cast<u8*>(reinterpret_cast<u64>(ptr) & -0x10000);
+#ifdef CHIMERA_CORE
+		memory_decommit(target, m_size, false);
+		return;
+#endif
 
 #ifdef _WIN32
 		if (is_memory_mappping_memory(ptr))
